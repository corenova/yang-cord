swagger: '2.0'
info:
  title: VSG Service Controller
  description: |
    YANG model-driven VSG Service Controller for CORD
  version: '1.0'
  contact:
    name: Peter Lee
    url: 'http://github.com/corenova/yang-cord'
    email: peter@corenova.com
  license:
    name: Apache-2.0
consumes:
  - application/json
produces:
  - application/json
paths:
  '/cord-vsg-service:controller':
    get:
      description: |-
        The controller configuration represents a VSG controller which manages
        multiple VSG gateways. The VSG controller provides agregate
        abstraction of the entire NFaaS as a single switch to the
        network fabric. Each port entry of the controller represents
        each VSG subscriber flow as a distinct openflow port.
      summary: View detail on controller
      deprecated: false
      responses:
        '200':
          description: Expected response of controller
          schema:
            description: >-
              The controller configuration represents a VSG controller which
              manages

              multiple VSG gateways. The VSG controller provides agregate

              abstraction of the entire NFaaS as a single switch to the

              network fabric. Each port entry of the controller represents

              each VSG subscriber flow as a distinct openflow port.
            allOf:
              - $ref: '#/definitions/xos:service'
              - $ref: '#/definitions/gateways-list'
              - properties:
                  slice:
                    $ref: '#/definitions/xslice:slice'
                  port:
                    type: array
                    items:
                      description: >-
                        Each entry represents a VSG subscriber flow connected
                        across VSG

                        gateways.
                      type: object
                      required:
                        - id
                        - link
                      properties:
                        id:
                          description: OpenFlow Port ID
                          type: string
                          format: 'yang:uuid'
                        link:
                          type: string
                          format: leafref
    put:
      summary: Update details on controller
      deprecated: false
      parameters:
        - name: controller
          in: body
          description: >-
            The controller configuration represents a VSG controller which
            manages

            multiple VSG gateways. The VSG controller provides agregate

            abstraction of the entire NFaaS as a single switch to the

            network fabric. Each port entry of the controller represents

            each VSG subscriber flow as a distinct openflow port.
          schema:
            description: >-
              The controller configuration represents a VSG controller which
              manages

              multiple VSG gateways. The VSG controller provides agregate

              abstraction of the entire NFaaS as a single switch to the

              network fabric. Each port entry of the controller represents

              each VSG subscriber flow as a distinct openflow port.
            allOf:
              - $ref: '#/definitions/xos:service'
              - $ref: '#/definitions/gateways-list'
              - properties:
                  slice:
                    $ref: '#/definitions/xslice:slice'
                  port:
                    type: array
                    items:
                      description: >-
                        Each entry represents a VSG subscriber flow connected
                        across VSG

                        gateways.
                      type: object
                      required:
                        - id
                        - link
                      properties:
                        id:
                          description: OpenFlow Port ID
                          type: string
                          format: 'yang:uuid'
                        link:
                          type: string
                          format: leafref
          required: false
      responses:
        '200':
          description: Expected response of controller
          schema:
            description: >-
              The controller configuration represents a VSG controller which
              manages

              multiple VSG gateways. The VSG controller provides agregate

              abstraction of the entire NFaaS as a single switch to the

              network fabric. Each port entry of the controller represents

              each VSG subscriber flow as a distinct openflow port.
            allOf:
              - $ref: '#/definitions/xos:service'
              - $ref: '#/definitions/gateways-list'
              - properties:
                  slice:
                    $ref: '#/definitions/xslice:slice'
                  port:
                    type: array
                    items:
                      description: >-
                        Each entry represents a VSG subscriber flow connected
                        across VSG

                        gateways.
                      type: object
                      required:
                        - id
                        - link
                      properties:
                        id:
                          description: OpenFlow Port ID
                          type: string
                          format: 'yang:uuid'
                        link:
                          type: string
                          format: leafref
    patch:
      summary: Merge details on controller
      deprecated: false
      parameters:
        - name: controller
          in: body
          description: >-
            The controller configuration represents a VSG controller which
            manages

            multiple VSG gateways. The VSG controller provides agregate

            abstraction of the entire NFaaS as a single switch to the

            network fabric. Each port entry of the controller represents

            each VSG subscriber flow as a distinct openflow port.
          schema:
            description: >-
              The controller configuration represents a VSG controller which
              manages

              multiple VSG gateways. The VSG controller provides agregate

              abstraction of the entire NFaaS as a single switch to the

              network fabric. Each port entry of the controller represents

              each VSG subscriber flow as a distinct openflow port.
            allOf:
              - $ref: '#/definitions/xos:service'
              - $ref: '#/definitions/gateways-list'
              - properties:
                  slice:
                    $ref: '#/definitions/xslice:slice'
                  port:
                    type: array
                    items:
                      description: >-
                        Each entry represents a VSG subscriber flow connected
                        across VSG

                        gateways.
                      type: object
                      required:
                        - id
                        - link
                      properties:
                        id:
                          description: OpenFlow Port ID
                          type: string
                          format: 'yang:uuid'
                        link:
                          type: string
                          format: leafref
          required: false
      responses:
        '200':
          description: Expected response of controller
          schema:
            description: >-
              The controller configuration represents a VSG controller which
              manages

              multiple VSG gateways. The VSG controller provides agregate

              abstraction of the entire NFaaS as a single switch to the

              network fabric. Each port entry of the controller represents

              each VSG subscriber flow as a distinct openflow port.
            allOf:
              - $ref: '#/definitions/xos:service'
              - $ref: '#/definitions/gateways-list'
              - properties:
                  slice:
                    $ref: '#/definitions/xslice:slice'
                  port:
                    type: array
                    items:
                      description: >-
                        Each entry represents a VSG subscriber flow connected
                        across VSG

                        gateways.
                      type: object
                      required:
                        - id
                        - link
                      properties:
                        id:
                          description: OpenFlow Port ID
                          type: string
                          format: 'yang:uuid'
                        link:
                          type: string
                          format: leafref
    delete:
      summary: Delete controller from cord-vsg-service
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
  '/cord-vsg-service:controller/slice':
    get:
      summary: View detail on slice
      deprecated: false
      responses:
        '200':
          description: Expected response of slice
          schema:
            $ref: '#/definitions/xslice:slice'
    put:
      summary: Update details on slice
      deprecated: false
      parameters:
        - name: slice
          in: body
          schema:
            $ref: '#/definitions/xslice:slice'
          required: false
      responses:
        '200':
          description: Expected response of slice
          schema:
            $ref: '#/definitions/xslice:slice'
    patch:
      summary: Merge details on slice
      deprecated: false
      parameters:
        - name: slice
          in: body
          schema:
            $ref: '#/definitions/xslice:slice'
          required: false
      responses:
        '200':
          description: Expected response of slice
          schema:
            $ref: '#/definitions/xslice:slice'
    delete:
      summary: Delete slice from controller
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
  '/cord-vsg-service:controller/slice/policy':
    get:
      summary: View detail on policy
      deprecated: false
      responses:
        '200':
          description: Expected response of policy
          schema:
            type: object
            properties:
              max-instances:
                description: >-
                  The maximum number of VMs that this slice is allowed to
                  allocate
                type: string
                format: uint16
              default-flavor:
                type: string
                format: leafref
              default-image:
                type: string
                format: leafref
              default-node:
                type: string
                format: leafref
              mount-data-sets:
                default: GenBank
                type: string
                format: string
              default-isolation:
                default: vm
                type: string
                format: 'xtype:isolation'
    put:
      summary: Update details on policy
      deprecated: false
      parameters:
        - name: policy
          in: body
          schema:
            type: object
            properties:
              max-instances:
                description: >-
                  The maximum number of VMs that this slice is allowed to
                  allocate
                type: string
                format: uint16
              default-flavor:
                type: string
                format: leafref
              default-image:
                type: string
                format: leafref
              default-node:
                type: string
                format: leafref
              mount-data-sets:
                default: GenBank
                type: string
                format: string
              default-isolation:
                default: vm
                type: string
                format: 'xtype:isolation'
          required: false
      responses:
        '200':
          description: Expected response of policy
          schema:
            type: object
            properties:
              max-instances:
                description: >-
                  The maximum number of VMs that this slice is allowed to
                  allocate
                type: string
                format: uint16
              default-flavor:
                type: string
                format: leafref
              default-image:
                type: string
                format: leafref
              default-node:
                type: string
                format: leafref
              mount-data-sets:
                default: GenBank
                type: string
                format: string
              default-isolation:
                default: vm
                type: string
                format: 'xtype:isolation'
    patch:
      summary: Merge details on policy
      deprecated: false
      parameters:
        - name: policy
          in: body
          schema:
            type: object
            properties:
              max-instances:
                description: >-
                  The maximum number of VMs that this slice is allowed to
                  allocate
                type: string
                format: uint16
              default-flavor:
                type: string
                format: leafref
              default-image:
                type: string
                format: leafref
              default-node:
                type: string
                format: leafref
              mount-data-sets:
                default: GenBank
                type: string
                format: string
              default-isolation:
                default: vm
                type: string
                format: 'xtype:isolation'
          required: false
      responses:
        '200':
          description: Expected response of policy
          schema:
            type: object
            properties:
              max-instances:
                description: >-
                  The maximum number of VMs that this slice is allowed to
                  allocate
                type: string
                format: uint16
              default-flavor:
                type: string
                format: leafref
              default-image:
                type: string
                format: leafref
              default-node:
                type: string
                format: leafref
              mount-data-sets:
                default: GenBank
                type: string
                format: string
              default-isolation:
                default: vm
                type: string
                format: 'xtype:isolation'
    delete:
      summary: Delete policy from slice
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
  '/cord-vsg-service:controller/slice/credential':
    post:
      summary: Creates one or more new credential in slice
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
          required: false
      responses:
        '200':
          description: Expected response for creating credential(s) in collection
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
    get:
      summary: List all credentials from slice
      deprecated: false
      responses:
        '200':
          description: Expected response of credentials
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
    put:
      summary: Replace the entire credential collection
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the credential collection
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/slice/credential/{token}':
    get:
      summary: View detail on credential
      deprecated: false
      responses:
        '200':
          description: Expected response of credential
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
    put:
      summary: Update details on credential
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
          required: false
      responses:
        '200':
          description: Expected response of credential
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
    patch:
      summary: Merge details on credential
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
          required: false
      responses:
        '200':
          description: Expected response of credential
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
    delete:
      summary: Delete credential from slice
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: token
        in: path
        required: true
        description: A key uniquely identifying credential item
        type: string
        format: string
  '/cord-vsg-service:controller/port':
    post:
      description: |-
        Each entry represents a VSG subscriber flow connected across VSG
        gateways.
      summary: Creates one or more new port in controller
      deprecated: false
      parameters:
        - name: port
          in: body
          description: |-
            Each entry represents a VSG subscriber flow connected across VSG
            gateways.
          schema:
            type: array
            items:
              description: |-
                Each entry represents a VSG subscriber flow connected across VSG
                gateways.
              type: object
              required:
                - id
                - link
              properties:
                id:
                  description: OpenFlow Port ID
                  type: string
                  format: 'yang:uuid'
                link:
                  type: string
                  format: leafref
          required: false
      responses:
        '200':
          description: Expected response for creating port(s) in collection
          schema:
            type: array
            items:
              description: |-
                Each entry represents a VSG subscriber flow connected across VSG
                gateways.
              type: object
              required:
                - id
                - link
              properties:
                id:
                  description: OpenFlow Port ID
                  type: string
                  format: 'yang:uuid'
                link:
                  type: string
                  format: leafref
    get:
      summary: List all ports from controller
      deprecated: false
      responses:
        '200':
          description: Expected response of ports
          schema:
            type: array
            items:
              description: |-
                Each entry represents a VSG subscriber flow connected across VSG
                gateways.
              type: object
              required:
                - id
                - link
              properties:
                id:
                  description: OpenFlow Port ID
                  type: string
                  format: 'yang:uuid'
                link:
                  type: string
                  format: leafref
    put:
      summary: Replace the entire port collection
      deprecated: false
      parameters:
        - name: port
          in: body
          description: |-
            Each entry represents a VSG subscriber flow connected across VSG
            gateways.
          schema:
            type: array
            items:
              description: |-
                Each entry represents a VSG subscriber flow connected across VSG
                gateways.
              type: object
              required:
                - id
                - link
              properties:
                id:
                  description: OpenFlow Port ID
                  type: string
                  format: 'yang:uuid'
                link:
                  type: string
                  format: leafref
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the port collection
      deprecated: false
      parameters:
        - name: port
          in: body
          description: |-
            Each entry represents a VSG subscriber flow connected across VSG
            gateways.
          schema:
            type: array
            items:
              description: |-
                Each entry represents a VSG subscriber flow connected across VSG
                gateways.
              type: object
              required:
                - id
                - link
              properties:
                id:
                  description: OpenFlow Port ID
                  type: string
                  format: 'yang:uuid'
                link:
                  type: string
                  format: leafref
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/port/{id}':
    get:
      description: |-
        Each entry represents a VSG subscriber flow connected across VSG
        gateways.
      summary: View detail on port
      deprecated: false
      responses:
        '200':
          description: Expected response of port
          schema:
            description: |-
              Each entry represents a VSG subscriber flow connected across VSG
              gateways.
            type: object
            required:
              - id
              - link
            properties:
              id:
                description: OpenFlow Port ID
                type: string
                format: 'yang:uuid'
              link:
                type: string
                format: leafref
    put:
      summary: Update details on port
      deprecated: false
      parameters:
        - name: port
          in: body
          description: |-
            Each entry represents a VSG subscriber flow connected across VSG
            gateways.
          schema:
            description: |-
              Each entry represents a VSG subscriber flow connected across VSG
              gateways.
            type: object
            required:
              - id
              - link
            properties:
              id:
                description: OpenFlow Port ID
                type: string
                format: 'yang:uuid'
              link:
                type: string
                format: leafref
          required: false
      responses:
        '200':
          description: Expected response of port
          schema:
            description: |-
              Each entry represents a VSG subscriber flow connected across VSG
              gateways.
            type: object
            required:
              - id
              - link
            properties:
              id:
                description: OpenFlow Port ID
                type: string
                format: 'yang:uuid'
              link:
                type: string
                format: leafref
    patch:
      summary: Merge details on port
      deprecated: false
      parameters:
        - name: port
          in: body
          description: |-
            Each entry represents a VSG subscriber flow connected across VSG
            gateways.
          schema:
            description: |-
              Each entry represents a VSG subscriber flow connected across VSG
              gateways.
            type: object
            required:
              - id
              - link
            properties:
              id:
                description: OpenFlow Port ID
                type: string
                format: 'yang:uuid'
              link:
                type: string
                format: leafref
          required: false
      responses:
        '200':
          description: Expected response of port
          schema:
            description: |-
              Each entry represents a VSG subscriber flow connected across VSG
              gateways.
            type: object
            required:
              - id
              - link
            properties:
              id:
                description: OpenFlow Port ID
                type: string
                format: 'yang:uuid'
              link:
                type: string
                format: leafref
    delete:
      summary: Delete port from controller
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: id
        in: path
        required: true
        description: OpenFlow Port ID
        type: string
        format: 'yang:uuid'
  '/cord-vsg-service:controller/keypair':
    post:
      description: collection of public/private key pair(s)
      summary: Creates one or more new keypair in service
      deprecated: false
      parameters:
        - name: keypair
          in: body
          description: collection of public/private key pair(s)
          schema:
            type: array
            items:
              description: collection of public/private key pair(s)
              type: object
              properties:
                public:
                  type: string
                  format: string
                private:
                  type: string
                  format: string
          required: false
      responses:
        '200':
          description: Expected response for creating keypair(s) in collection
          schema:
            type: array
            items:
              description: collection of public/private key pair(s)
              type: object
              properties:
                public:
                  type: string
                  format: string
                private:
                  type: string
                  format: string
    get:
      summary: List all keypairs from service
      deprecated: false
      responses:
        '200':
          description: Expected response of keypairs
          schema:
            type: array
            items:
              description: collection of public/private key pair(s)
              type: object
              properties:
                public:
                  type: string
                  format: string
                private:
                  type: string
                  format: string
    put:
      summary: Replace the entire keypair collection
      deprecated: false
      parameters:
        - name: keypair
          in: body
          description: collection of public/private key pair(s)
          schema:
            type: array
            items:
              description: collection of public/private key pair(s)
              type: object
              properties:
                public:
                  type: string
                  format: string
                private:
                  type: string
                  format: string
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the keypair collection
      deprecated: false
      parameters:
        - name: keypair
          in: body
          description: collection of public/private key pair(s)
          schema:
            type: array
            items:
              description: collection of public/private key pair(s)
              type: object
              properties:
                public:
                  type: string
                  format: string
                private:
                  type: string
                  format: string
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/keypair/{index}':
    get:
      description: collection of public/private key pair(s)
      summary: View detail on keypair
      deprecated: false
      responses:
        '200':
          description: Expected response of keypair
          schema:
            description: collection of public/private key pair(s)
            type: object
            properties:
              public:
                type: string
                format: string
              private:
                type: string
                format: string
    put:
      summary: Update details on keypair
      deprecated: false
      parameters:
        - name: keypair
          in: body
          description: collection of public/private key pair(s)
          schema:
            description: collection of public/private key pair(s)
            type: object
            properties:
              public:
                type: string
                format: string
              private:
                type: string
                format: string
          required: false
      responses:
        '200':
          description: Expected response of keypair
          schema:
            description: collection of public/private key pair(s)
            type: object
            properties:
              public:
                type: string
                format: string
              private:
                type: string
                format: string
    patch:
      summary: Merge details on keypair
      deprecated: false
      parameters:
        - name: keypair
          in: body
          description: collection of public/private key pair(s)
          schema:
            description: collection of public/private key pair(s)
            type: object
            properties:
              public:
                type: string
                format: string
              private:
                type: string
                format: string
          required: false
      responses:
        '200':
          description: Expected response of keypair
          schema:
            description: collection of public/private key pair(s)
            type: object
            properties:
              public:
                type: string
                format: string
              private:
                type: string
                format: string
    delete:
      summary: Delete keypair from service
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: index
        in: path
        required: true
        type: integer
        format: int64
        description: An index key identifying keypair item (may change over time)
  '/cord-vsg-service:controller/attribute':
    post:
      description: backwards-compatible attribute association
      summary: Creates one or more new attribute in service
      deprecated: true
      parameters:
        - name: attribute
          in: body
          description: backwards-compatible attribute association
          schema:
            type: array
            items:
              description: backwards-compatible attribute association
              type: object
              properties:
                name:
                  type: string
                  format: string
                value:
                  type: string
                  format: string
          required: false
      responses:
        '200':
          description: Expected response for creating attribute(s) in collection
          schema:
            type: array
            items:
              description: backwards-compatible attribute association
              type: object
              properties:
                name:
                  type: string
                  format: string
                value:
                  type: string
                  format: string
    get:
      summary: List all attributes from service
      deprecated: true
      responses:
        '200':
          description: Expected response of attributes
          schema:
            type: array
            items:
              description: backwards-compatible attribute association
              type: object
              properties:
                name:
                  type: string
                  format: string
                value:
                  type: string
                  format: string
    put:
      summary: Replace the entire attribute collection
      deprecated: true
      parameters:
        - name: attribute
          in: body
          description: backwards-compatible attribute association
          schema:
            type: array
            items:
              description: backwards-compatible attribute association
              type: object
              properties:
                name:
                  type: string
                  format: string
                value:
                  type: string
                  format: string
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the attribute collection
      deprecated: true
      parameters:
        - name: attribute
          in: body
          description: backwards-compatible attribute association
          schema:
            type: array
            items:
              description: backwards-compatible attribute association
              type: object
              properties:
                name:
                  type: string
                  format: string
                value:
                  type: string
                  format: string
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/attribute/{name}':
    get:
      description: backwards-compatible attribute association
      summary: View detail on attribute
      deprecated: true
      responses:
        '200':
          description: Expected response of attribute
          schema:
            description: backwards-compatible attribute association
            type: object
            properties:
              name:
                type: string
                format: string
              value:
                type: string
                format: string
    put:
      summary: Update details on attribute
      deprecated: true
      parameters:
        - name: attribute
          in: body
          description: backwards-compatible attribute association
          schema:
            description: backwards-compatible attribute association
            type: object
            properties:
              name:
                type: string
                format: string
              value:
                type: string
                format: string
          required: false
      responses:
        '200':
          description: Expected response of attribute
          schema:
            description: backwards-compatible attribute association
            type: object
            properties:
              name:
                type: string
                format: string
              value:
                type: string
                format: string
    patch:
      summary: Merge details on attribute
      deprecated: true
      parameters:
        - name: attribute
          in: body
          description: backwards-compatible attribute association
          schema:
            description: backwards-compatible attribute association
            type: object
            properties:
              name:
                type: string
                format: string
              value:
                type: string
                format: string
          required: false
      responses:
        '200':
          description: Expected response of attribute
          schema:
            description: backwards-compatible attribute association
            type: object
            properties:
              name:
                type: string
                format: string
              value:
                type: string
                format: string
    delete:
      summary: Delete attribute from service
      deprecated: true
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: name
        in: path
        required: true
        description: A key uniquely identifying attribute item
        type: string
        format: string
  '/cord-vsg-service:controller/subscriber':
    post:
      description: |-
        Each entry represents a subscriber of the service. Each unique service
        provider should augment this block with service specific
        attributes.
      summary: Creates one or more new subscriber in service
      deprecated: false
      parameters:
        - name: subscriber
          in: body
          description: >-
            Each entry represents a subscriber of the service. Each unique
            service

            provider should augment this block with service specific

            attributes.
          schema:
            type: array
            items:
              description: >-
                Each entry represents a subscriber of the service. Each unique
                service

                provider should augment this block with service specific

                attributes.
              allOf:
                - $ref: '#/definitions/xos:subscriber'
                - $ref: '#/definitions/xos:tenant'
                - required:
                    - id
                  properties:
                    connectivity:
                      default: na
                      type: string
                      format: enumeration
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    name:
                      type: string
                      format: string
                    kind:
                      default: subscriber
                      type: string
                      format: identityref
                    description:
                      type: string
                      format: 'meta:description'
                    enabled:
                      default: 'true'
                      type: string
                      format: boolean
                    record:
                      allOf:
                        - $ref: '#/definitions/xos:record'
                        - properties:
                            created:
                              type: string
                              format: 'yang:date-and-time'
                            updated:
                              type: string
                              format: 'yang:date-and-time'
                            enacted:
                              type: string
                              format: 'yang:date-and-time'
                            policed:
                              type: string
                              format: 'yang:date-and-time'
                            writable:
                              default: 'true'
                              type: string
                              format: boolean
                            locked:
                              default: 'false'
                              type: string
                              format: boolean
                            deleted:
                              default: 'false'
                              type: string
                              format: boolean
                            dirty:
                              default: 'false'
                              type: string
                              format: boolean
                            sync:
                              type: object
                              properties:
                                progress:
                                  type: string
                                  format: enumeration
                                disabled:
                                  default: 'false'
                                  type: string
                                  format: boolean
                                enforced:
                                  default: 'true'
                                  type: string
                                  format: boolean
                                policy:
                                  type: array
                                  items:
                                    type: object
                            diff:
                              description: retrieve diff of model state if dirty
                              type: object
                            save:
                              description: trigger save into data store via synchronizer
                              type: object
          required: false
      responses:
        '200':
          description: Expected response for creating subscriber(s) in collection
          schema:
            type: array
            items:
              description: >-
                Each entry represents a subscriber of the service. Each unique
                service

                provider should augment this block with service specific

                attributes.
              allOf:
                - $ref: '#/definitions/xos:subscriber'
                - $ref: '#/definitions/xos:tenant'
                - required:
                    - id
                  properties:
                    connectivity:
                      default: na
                      type: string
                      format: enumeration
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    name:
                      type: string
                      format: string
                    kind:
                      default: subscriber
                      type: string
                      format: identityref
                    description:
                      type: string
                      format: 'meta:description'
                    enabled:
                      default: 'true'
                      type: string
                      format: boolean
                    record:
                      allOf:
                        - $ref: '#/definitions/xos:record'
                        - properties:
                            created:
                              type: string
                              format: 'yang:date-and-time'
                            updated:
                              type: string
                              format: 'yang:date-and-time'
                            enacted:
                              type: string
                              format: 'yang:date-and-time'
                            policed:
                              type: string
                              format: 'yang:date-and-time'
                            writable:
                              default: 'true'
                              type: string
                              format: boolean
                            locked:
                              default: 'false'
                              type: string
                              format: boolean
                            deleted:
                              default: 'false'
                              type: string
                              format: boolean
                            dirty:
                              default: 'false'
                              type: string
                              format: boolean
                            sync:
                              type: object
                              properties:
                                progress:
                                  type: string
                                  format: enumeration
                                disabled:
                                  default: 'false'
                                  type: string
                                  format: boolean
                                enforced:
                                  default: 'true'
                                  type: string
                                  format: boolean
                                policy:
                                  type: array
                                  items:
                                    type: object
                            diff:
                              description: retrieve diff of model state if dirty
                              type: object
                            save:
                              description: trigger save into data store via synchronizer
                              type: object
    get:
      summary: List all subscribers from service
      deprecated: false
      responses:
        '200':
          description: Expected response of subscribers
          schema:
            type: array
            items:
              description: >-
                Each entry represents a subscriber of the service. Each unique
                service

                provider should augment this block with service specific

                attributes.
              allOf:
                - $ref: '#/definitions/xos:subscriber'
                - $ref: '#/definitions/xos:tenant'
                - required:
                    - id
                  properties:
                    connectivity:
                      default: na
                      type: string
                      format: enumeration
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    name:
                      type: string
                      format: string
                    kind:
                      default: subscriber
                      type: string
                      format: identityref
                    description:
                      type: string
                      format: 'meta:description'
                    enabled:
                      default: 'true'
                      type: string
                      format: boolean
                    record:
                      allOf:
                        - $ref: '#/definitions/xos:record'
                        - properties:
                            created:
                              type: string
                              format: 'yang:date-and-time'
                            updated:
                              type: string
                              format: 'yang:date-and-time'
                            enacted:
                              type: string
                              format: 'yang:date-and-time'
                            policed:
                              type: string
                              format: 'yang:date-and-time'
                            writable:
                              default: 'true'
                              type: string
                              format: boolean
                            locked:
                              default: 'false'
                              type: string
                              format: boolean
                            deleted:
                              default: 'false'
                              type: string
                              format: boolean
                            dirty:
                              default: 'false'
                              type: string
                              format: boolean
                            sync:
                              type: object
                              properties:
                                progress:
                                  type: string
                                  format: enumeration
                                disabled:
                                  default: 'false'
                                  type: string
                                  format: boolean
                                enforced:
                                  default: 'true'
                                  type: string
                                  format: boolean
                                policy:
                                  type: array
                                  items:
                                    type: object
                            diff:
                              description: retrieve diff of model state if dirty
                              type: object
                            save:
                              description: trigger save into data store via synchronizer
                              type: object
    put:
      summary: Replace the entire subscriber collection
      deprecated: false
      parameters:
        - name: subscriber
          in: body
          description: >-
            Each entry represents a subscriber of the service. Each unique
            service

            provider should augment this block with service specific

            attributes.
          schema:
            type: array
            items:
              description: >-
                Each entry represents a subscriber of the service. Each unique
                service

                provider should augment this block with service specific

                attributes.
              allOf:
                - $ref: '#/definitions/xos:subscriber'
                - $ref: '#/definitions/xos:tenant'
                - required:
                    - id
                  properties:
                    connectivity:
                      default: na
                      type: string
                      format: enumeration
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    name:
                      type: string
                      format: string
                    kind:
                      default: subscriber
                      type: string
                      format: identityref
                    description:
                      type: string
                      format: 'meta:description'
                    enabled:
                      default: 'true'
                      type: string
                      format: boolean
                    record:
                      allOf:
                        - $ref: '#/definitions/xos:record'
                        - properties:
                            created:
                              type: string
                              format: 'yang:date-and-time'
                            updated:
                              type: string
                              format: 'yang:date-and-time'
                            enacted:
                              type: string
                              format: 'yang:date-and-time'
                            policed:
                              type: string
                              format: 'yang:date-and-time'
                            writable:
                              default: 'true'
                              type: string
                              format: boolean
                            locked:
                              default: 'false'
                              type: string
                              format: boolean
                            deleted:
                              default: 'false'
                              type: string
                              format: boolean
                            dirty:
                              default: 'false'
                              type: string
                              format: boolean
                            sync:
                              type: object
                              properties:
                                progress:
                                  type: string
                                  format: enumeration
                                disabled:
                                  default: 'false'
                                  type: string
                                  format: boolean
                                enforced:
                                  default: 'true'
                                  type: string
                                  format: boolean
                                policy:
                                  type: array
                                  items:
                                    type: object
                            diff:
                              description: retrieve diff of model state if dirty
                              type: object
                            save:
                              description: trigger save into data store via synchronizer
                              type: object
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the subscriber collection
      deprecated: false
      parameters:
        - name: subscriber
          in: body
          description: >-
            Each entry represents a subscriber of the service. Each unique
            service

            provider should augment this block with service specific

            attributes.
          schema:
            type: array
            items:
              description: >-
                Each entry represents a subscriber of the service. Each unique
                service

                provider should augment this block with service specific

                attributes.
              allOf:
                - $ref: '#/definitions/xos:subscriber'
                - $ref: '#/definitions/xos:tenant'
                - required:
                    - id
                  properties:
                    connectivity:
                      default: na
                      type: string
                      format: enumeration
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    name:
                      type: string
                      format: string
                    kind:
                      default: subscriber
                      type: string
                      format: identityref
                    description:
                      type: string
                      format: 'meta:description'
                    enabled:
                      default: 'true'
                      type: string
                      format: boolean
                    record:
                      allOf:
                        - $ref: '#/definitions/xos:record'
                        - properties:
                            created:
                              type: string
                              format: 'yang:date-and-time'
                            updated:
                              type: string
                              format: 'yang:date-and-time'
                            enacted:
                              type: string
                              format: 'yang:date-and-time'
                            policed:
                              type: string
                              format: 'yang:date-and-time'
                            writable:
                              default: 'true'
                              type: string
                              format: boolean
                            locked:
                              default: 'false'
                              type: string
                              format: boolean
                            deleted:
                              default: 'false'
                              type: string
                              format: boolean
                            dirty:
                              default: 'false'
                              type: string
                              format: boolean
                            sync:
                              type: object
                              properties:
                                progress:
                                  type: string
                                  format: enumeration
                                disabled:
                                  default: 'false'
                                  type: string
                                  format: boolean
                                enforced:
                                  default: 'true'
                                  type: string
                                  format: boolean
                                policy:
                                  type: array
                                  items:
                                    type: object
                            diff:
                              description: retrieve diff of model state if dirty
                              type: object
                            save:
                              description: trigger save into data store via synchronizer
                              type: object
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/subscriber/{id}':
    get:
      description: |-
        Each entry represents a subscriber of the service. Each unique service
        provider should augment this block with service specific
        attributes.
      summary: View detail on subscriber
      deprecated: false
      responses:
        '200':
          description: Expected response of subscriber
          schema:
            description: >-
              Each entry represents a subscriber of the service. Each unique
              service

              provider should augment this block with service specific

              attributes.
            allOf:
              - $ref: '#/definitions/xos:subscriber'
              - $ref: '#/definitions/xos:tenant'
              - required:
                  - id
                properties:
                  connectivity:
                    default: na
                    type: string
                    format: enumeration
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  name:
                    type: string
                    format: string
                  kind:
                    default: subscriber
                    type: string
                    format: identityref
                  description:
                    type: string
                    format: 'meta:description'
                  enabled:
                    default: 'true'
                    type: string
                    format: boolean
                  record:
                    allOf:
                      - $ref: '#/definitions/xos:record'
                      - properties:
                          created:
                            type: string
                            format: 'yang:date-and-time'
                          updated:
                            type: string
                            format: 'yang:date-and-time'
                          enacted:
                            type: string
                            format: 'yang:date-and-time'
                          policed:
                            type: string
                            format: 'yang:date-and-time'
                          writable:
                            default: 'true'
                            type: string
                            format: boolean
                          locked:
                            default: 'false'
                            type: string
                            format: boolean
                          deleted:
                            default: 'false'
                            type: string
                            format: boolean
                          dirty:
                            default: 'false'
                            type: string
                            format: boolean
                          sync:
                            type: object
                            properties:
                              progress:
                                type: string
                                format: enumeration
                              disabled:
                                default: 'false'
                                type: string
                                format: boolean
                              enforced:
                                default: 'true'
                                type: string
                                format: boolean
                              policy:
                                type: array
                                items:
                                  type: object
                          diff:
                            description: retrieve diff of model state if dirty
                            type: object
                          save:
                            description: trigger save into data store via synchronizer
                            type: object
    put:
      summary: Update details on subscriber
      deprecated: false
      parameters:
        - name: subscriber
          in: body
          description: >-
            Each entry represents a subscriber of the service. Each unique
            service

            provider should augment this block with service specific

            attributes.
          schema:
            description: >-
              Each entry represents a subscriber of the service. Each unique
              service

              provider should augment this block with service specific

              attributes.
            allOf:
              - $ref: '#/definitions/xos:subscriber'
              - $ref: '#/definitions/xos:tenant'
              - required:
                  - id
                properties:
                  connectivity:
                    default: na
                    type: string
                    format: enumeration
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  name:
                    type: string
                    format: string
                  kind:
                    default: subscriber
                    type: string
                    format: identityref
                  description:
                    type: string
                    format: 'meta:description'
                  enabled:
                    default: 'true'
                    type: string
                    format: boolean
                  record:
                    allOf:
                      - $ref: '#/definitions/xos:record'
                      - properties:
                          created:
                            type: string
                            format: 'yang:date-and-time'
                          updated:
                            type: string
                            format: 'yang:date-and-time'
                          enacted:
                            type: string
                            format: 'yang:date-and-time'
                          policed:
                            type: string
                            format: 'yang:date-and-time'
                          writable:
                            default: 'true'
                            type: string
                            format: boolean
                          locked:
                            default: 'false'
                            type: string
                            format: boolean
                          deleted:
                            default: 'false'
                            type: string
                            format: boolean
                          dirty:
                            default: 'false'
                            type: string
                            format: boolean
                          sync:
                            type: object
                            properties:
                              progress:
                                type: string
                                format: enumeration
                              disabled:
                                default: 'false'
                                type: string
                                format: boolean
                              enforced:
                                default: 'true'
                                type: string
                                format: boolean
                              policy:
                                type: array
                                items:
                                  type: object
                          diff:
                            description: retrieve diff of model state if dirty
                            type: object
                          save:
                            description: trigger save into data store via synchronizer
                            type: object
          required: false
      responses:
        '200':
          description: Expected response of subscriber
          schema:
            description: >-
              Each entry represents a subscriber of the service. Each unique
              service

              provider should augment this block with service specific

              attributes.
            allOf:
              - $ref: '#/definitions/xos:subscriber'
              - $ref: '#/definitions/xos:tenant'
              - required:
                  - id
                properties:
                  connectivity:
                    default: na
                    type: string
                    format: enumeration
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  name:
                    type: string
                    format: string
                  kind:
                    default: subscriber
                    type: string
                    format: identityref
                  description:
                    type: string
                    format: 'meta:description'
                  enabled:
                    default: 'true'
                    type: string
                    format: boolean
                  record:
                    allOf:
                      - $ref: '#/definitions/xos:record'
                      - properties:
                          created:
                            type: string
                            format: 'yang:date-and-time'
                          updated:
                            type: string
                            format: 'yang:date-and-time'
                          enacted:
                            type: string
                            format: 'yang:date-and-time'
                          policed:
                            type: string
                            format: 'yang:date-and-time'
                          writable:
                            default: 'true'
                            type: string
                            format: boolean
                          locked:
                            default: 'false'
                            type: string
                            format: boolean
                          deleted:
                            default: 'false'
                            type: string
                            format: boolean
                          dirty:
                            default: 'false'
                            type: string
                            format: boolean
                          sync:
                            type: object
                            properties:
                              progress:
                                type: string
                                format: enumeration
                              disabled:
                                default: 'false'
                                type: string
                                format: boolean
                              enforced:
                                default: 'true'
                                type: string
                                format: boolean
                              policy:
                                type: array
                                items:
                                  type: object
                          diff:
                            description: retrieve diff of model state if dirty
                            type: object
                          save:
                            description: trigger save into data store via synchronizer
                            type: object
    patch:
      summary: Merge details on subscriber
      deprecated: false
      parameters:
        - name: subscriber
          in: body
          description: >-
            Each entry represents a subscriber of the service. Each unique
            service

            provider should augment this block with service specific

            attributes.
          schema:
            description: >-
              Each entry represents a subscriber of the service. Each unique
              service

              provider should augment this block with service specific

              attributes.
            allOf:
              - $ref: '#/definitions/xos:subscriber'
              - $ref: '#/definitions/xos:tenant'
              - required:
                  - id
                properties:
                  connectivity:
                    default: na
                    type: string
                    format: enumeration
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  name:
                    type: string
                    format: string
                  kind:
                    default: subscriber
                    type: string
                    format: identityref
                  description:
                    type: string
                    format: 'meta:description'
                  enabled:
                    default: 'true'
                    type: string
                    format: boolean
                  record:
                    allOf:
                      - $ref: '#/definitions/xos:record'
                      - properties:
                          created:
                            type: string
                            format: 'yang:date-and-time'
                          updated:
                            type: string
                            format: 'yang:date-and-time'
                          enacted:
                            type: string
                            format: 'yang:date-and-time'
                          policed:
                            type: string
                            format: 'yang:date-and-time'
                          writable:
                            default: 'true'
                            type: string
                            format: boolean
                          locked:
                            default: 'false'
                            type: string
                            format: boolean
                          deleted:
                            default: 'false'
                            type: string
                            format: boolean
                          dirty:
                            default: 'false'
                            type: string
                            format: boolean
                          sync:
                            type: object
                            properties:
                              progress:
                                type: string
                                format: enumeration
                              disabled:
                                default: 'false'
                                type: string
                                format: boolean
                              enforced:
                                default: 'true'
                                type: string
                                format: boolean
                              policy:
                                type: array
                                items:
                                  type: object
                          diff:
                            description: retrieve diff of model state if dirty
                            type: object
                          save:
                            description: trigger save into data store via synchronizer
                            type: object
          required: false
      responses:
        '200':
          description: Expected response of subscriber
          schema:
            description: >-
              Each entry represents a subscriber of the service. Each unique
              service

              provider should augment this block with service specific

              attributes.
            allOf:
              - $ref: '#/definitions/xos:subscriber'
              - $ref: '#/definitions/xos:tenant'
              - required:
                  - id
                properties:
                  connectivity:
                    default: na
                    type: string
                    format: enumeration
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  name:
                    type: string
                    format: string
                  kind:
                    default: subscriber
                    type: string
                    format: identityref
                  description:
                    type: string
                    format: 'meta:description'
                  enabled:
                    default: 'true'
                    type: string
                    format: boolean
                  record:
                    allOf:
                      - $ref: '#/definitions/xos:record'
                      - properties:
                          created:
                            type: string
                            format: 'yang:date-and-time'
                          updated:
                            type: string
                            format: 'yang:date-and-time'
                          enacted:
                            type: string
                            format: 'yang:date-and-time'
                          policed:
                            type: string
                            format: 'yang:date-and-time'
                          writable:
                            default: 'true'
                            type: string
                            format: boolean
                          locked:
                            default: 'false'
                            type: string
                            format: boolean
                          deleted:
                            default: 'false'
                            type: string
                            format: boolean
                          dirty:
                            default: 'false'
                            type: string
                            format: boolean
                          sync:
                            type: object
                            properties:
                              progress:
                                type: string
                                format: enumeration
                              disabled:
                                default: 'false'
                                type: string
                                format: boolean
                              enforced:
                                default: 'true'
                                type: string
                                format: boolean
                              policy:
                                type: array
                                items:
                                  type: object
                          diff:
                            description: retrieve diff of model state if dirty
                            type: object
                          save:
                            description: trigger save into data store via synchronizer
                            type: object
    delete:
      summary: Delete subscriber from service
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: id
        in: path
        required: true
        description: A key uniquely identifying subscriber item
        type: string
        format: 'xtype:unique-identifier'
  '/cord-vsg-service:controller/credential':
    post:
      summary: Creates one or more new credential in service
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
          required: false
      responses:
        '200':
          description: Expected response for creating credential(s) in collection
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
    get:
      summary: List all credentials from service
      deprecated: false
      responses:
        '200':
          description: Expected response of credentials
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
    put:
      summary: Replace the entire credential collection
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the credential collection
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: array
            items:
              type: object
              properties:
                token:
                  type: string
                  format: string
                password:
                  type: string
                  format: 'meta:password'
                user:
                  type: string
                  format: 'xtype:unique-identifier'
                role:
                  default: user
                  type: string
                  format: 'xtype:access-role'
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/credential/{token}':
    get:
      summary: View detail on credential
      deprecated: false
      responses:
        '200':
          description: Expected response of credential
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
    put:
      summary: Update details on credential
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
          required: false
      responses:
        '200':
          description: Expected response of credential
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
    patch:
      summary: Merge details on credential
      deprecated: false
      parameters:
        - name: credential
          in: body
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
          required: false
      responses:
        '200':
          description: Expected response of credential
          schema:
            type: object
            properties:
              token:
                type: string
                format: string
              password:
                type: string
                format: 'meta:password'
              user:
                type: string
                format: 'xtype:unique-identifier'
              role:
                default: user
                type: string
                format: 'xtype:access-role'
    delete:
      summary: Delete credential from service
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: token
        in: path
        required: true
        description: A key uniquely identifying credential item
        type: string
        format: string
  '/cord-vsg-service:controller/gateway':
    post:
      description: Each entry represents a VSG instance.
      summary: Creates one or more new gateway in gateways-list
      deprecated: false
      parameters:
        - name: gateway
          in: body
          description: Each entry represents a VSG instance.
          schema:
            type: array
            items:
              description: Each entry represents a VSG instance.
              allOf:
                - $ref: '#/definitions/subscriber-gateway'
                - properties:
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    features:
                      description: Contains various features provided by the gateway.
                      type: object
                      properties:
                        firewall:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        url-filter:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            level:
                              type: string
                              format: enumeration
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        uverse:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                    flow:
                      type: array
                      items:
                        description: >-
                          Each flow represents a subscriber flow into the VSG
                          instance.
                        type: object
                        properties:
                          id:
                            type: string
                            format: 'yang:uuid'
          required: false
      responses:
        '200':
          description: Expected response for creating gateway(s) in collection
          schema:
            type: array
            items:
              description: Each entry represents a VSG instance.
              allOf:
                - $ref: '#/definitions/subscriber-gateway'
                - properties:
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    features:
                      description: Contains various features provided by the gateway.
                      type: object
                      properties:
                        firewall:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        url-filter:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            level:
                              type: string
                              format: enumeration
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        uverse:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                    flow:
                      type: array
                      items:
                        description: >-
                          Each flow represents a subscriber flow into the VSG
                          instance.
                        type: object
                        properties:
                          id:
                            type: string
                            format: 'yang:uuid'
    get:
      summary: List all gateways from gateways-list
      deprecated: false
      responses:
        '200':
          description: Expected response of gateways
          schema:
            type: array
            items:
              description: Each entry represents a VSG instance.
              allOf:
                - $ref: '#/definitions/subscriber-gateway'
                - properties:
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    features:
                      description: Contains various features provided by the gateway.
                      type: object
                      properties:
                        firewall:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        url-filter:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            level:
                              type: string
                              format: enumeration
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        uverse:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                    flow:
                      type: array
                      items:
                        description: >-
                          Each flow represents a subscriber flow into the VSG
                          instance.
                        type: object
                        properties:
                          id:
                            type: string
                            format: 'yang:uuid'
    put:
      summary: Replace the entire gateway collection
      deprecated: false
      parameters:
        - name: gateway
          in: body
          description: Each entry represents a VSG instance.
          schema:
            type: array
            items:
              description: Each entry represents a VSG instance.
              allOf:
                - $ref: '#/definitions/subscriber-gateway'
                - properties:
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    features:
                      description: Contains various features provided by the gateway.
                      type: object
                      properties:
                        firewall:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        url-filter:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            level:
                              type: string
                              format: enumeration
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        uverse:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                    flow:
                      type: array
                      items:
                        description: >-
                          Each flow represents a subscriber flow into the VSG
                          instance.
                        type: object
                        properties:
                          id:
                            type: string
                            format: 'yang:uuid'
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the gateway collection
      deprecated: false
      parameters:
        - name: gateway
          in: body
          description: Each entry represents a VSG instance.
          schema:
            type: array
            items:
              description: Each entry represents a VSG instance.
              allOf:
                - $ref: '#/definitions/subscriber-gateway'
                - properties:
                    id:
                      type: string
                      format: 'xtype:unique-identifier'
                    features:
                      description: Contains various features provided by the gateway.
                      type: object
                      properties:
                        firewall:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        url-filter:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                            level:
                              type: string
                              format: enumeration
                            rules:
                              type: array
                              items:
                                type: string
                                format: string
                        uverse:
                          type: object
                          properties:
                            enabled:
                              default: 'false'
                              type: string
                              format: boolean
                    flow:
                      type: array
                      items:
                        description: >-
                          Each flow represents a subscriber flow into the VSG
                          instance.
                        type: object
                        properties:
                          id:
                            type: string
                            format: 'yang:uuid'
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
  '/cord-vsg-service:controller/gateway/{id}':
    get:
      description: Each entry represents a VSG instance.
      summary: View detail on gateway
      deprecated: false
      responses:
        '200':
          description: Expected response of gateway
          schema:
            description: Each entry represents a VSG instance.
            allOf:
              - $ref: '#/definitions/subscriber-gateway'
              - properties:
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  features:
                    description: Contains various features provided by the gateway.
                    type: object
                    properties:
                      firewall:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      url-filter:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          level:
                            type: string
                            format: enumeration
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      uverse:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                  flow:
                    type: array
                    items:
                      description: >-
                        Each flow represents a subscriber flow into the VSG
                        instance.
                      type: object
                      properties:
                        id:
                          type: string
                          format: 'yang:uuid'
    put:
      summary: Update details on gateway
      deprecated: false
      parameters:
        - name: gateway
          in: body
          description: Each entry represents a VSG instance.
          schema:
            description: Each entry represents a VSG instance.
            allOf:
              - $ref: '#/definitions/subscriber-gateway'
              - properties:
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  features:
                    description: Contains various features provided by the gateway.
                    type: object
                    properties:
                      firewall:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      url-filter:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          level:
                            type: string
                            format: enumeration
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      uverse:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                  flow:
                    type: array
                    items:
                      description: >-
                        Each flow represents a subscriber flow into the VSG
                        instance.
                      type: object
                      properties:
                        id:
                          type: string
                          format: 'yang:uuid'
          required: false
      responses:
        '200':
          description: Expected response of gateway
          schema:
            description: Each entry represents a VSG instance.
            allOf:
              - $ref: '#/definitions/subscriber-gateway'
              - properties:
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  features:
                    description: Contains various features provided by the gateway.
                    type: object
                    properties:
                      firewall:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      url-filter:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          level:
                            type: string
                            format: enumeration
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      uverse:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                  flow:
                    type: array
                    items:
                      description: >-
                        Each flow represents a subscriber flow into the VSG
                        instance.
                      type: object
                      properties:
                        id:
                          type: string
                          format: 'yang:uuid'
    patch:
      summary: Merge details on gateway
      deprecated: false
      parameters:
        - name: gateway
          in: body
          description: Each entry represents a VSG instance.
          schema:
            description: Each entry represents a VSG instance.
            allOf:
              - $ref: '#/definitions/subscriber-gateway'
              - properties:
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  features:
                    description: Contains various features provided by the gateway.
                    type: object
                    properties:
                      firewall:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      url-filter:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          level:
                            type: string
                            format: enumeration
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      uverse:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                  flow:
                    type: array
                    items:
                      description: >-
                        Each flow represents a subscriber flow into the VSG
                        instance.
                      type: object
                      properties:
                        id:
                          type: string
                          format: 'yang:uuid'
          required: false
      responses:
        '200':
          description: Expected response of gateway
          schema:
            description: Each entry represents a VSG instance.
            allOf:
              - $ref: '#/definitions/subscriber-gateway'
              - properties:
                  id:
                    type: string
                    format: 'xtype:unique-identifier'
                  features:
                    description: Contains various features provided by the gateway.
                    type: object
                    properties:
                      firewall:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      url-filter:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                          level:
                            type: string
                            format: enumeration
                          rules:
                            type: array
                            items:
                              type: string
                              format: string
                      uverse:
                        type: object
                        properties:
                          enabled:
                            default: 'false'
                            type: string
                            format: boolean
                  flow:
                    type: array
                    items:
                      description: >-
                        Each flow represents a subscriber flow into the VSG
                        instance.
                      type: object
                      properties:
                        id:
                          type: string
                          format: 'yang:uuid'
    delete:
      summary: Delete gateway from gateways-list
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: id
        in: path
        required: true
        description: A key uniquely identifying gateway item
        type: string
        format: 'xtype:unique-identifier'
  '/cord-vsg-service:controller/gateway/{id}/features':
    get:
      description: Contains various features provided by the gateway.
      summary: View detail on features
      deprecated: false
      responses:
        '200':
          description: Expected response of features
          schema:
            description: Contains various features provided by the gateway.
            type: object
            properties:
              firewall:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              url-filter:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  level:
                    type: string
                    format: enumeration
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              uverse:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
    put:
      summary: Update details on features
      deprecated: false
      parameters:
        - name: features
          in: body
          description: Contains various features provided by the gateway.
          schema:
            description: Contains various features provided by the gateway.
            type: object
            properties:
              firewall:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              url-filter:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  level:
                    type: string
                    format: enumeration
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              uverse:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
          required: false
      responses:
        '200':
          description: Expected response of features
          schema:
            description: Contains various features provided by the gateway.
            type: object
            properties:
              firewall:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              url-filter:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  level:
                    type: string
                    format: enumeration
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              uverse:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
    patch:
      summary: Merge details on features
      deprecated: false
      parameters:
        - name: features
          in: body
          description: Contains various features provided by the gateway.
          schema:
            description: Contains various features provided by the gateway.
            type: object
            properties:
              firewall:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              url-filter:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  level:
                    type: string
                    format: enumeration
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              uverse:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
          required: false
      responses:
        '200':
          description: Expected response of features
          schema:
            description: Contains various features provided by the gateway.
            type: object
            properties:
              firewall:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              url-filter:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
                  level:
                    type: string
                    format: enumeration
                  rules:
                    type: array
                    items:
                      type: string
                      format: string
              uverse:
                type: object
                properties:
                  enabled:
                    default: 'false'
                    type: string
                    format: boolean
    delete:
      summary: Delete features from gateway
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: id
        in: path
        required: true
        description: A key uniquely identifying gateway item
        type: string
        format: 'xtype:unique-identifier'
  '/cord-vsg-service:controller/gateway/{id}/flow':
    post:
      description: Each flow represents a subscriber flow into the VSG instance.
      summary: Creates one or more new flow in gateway
      deprecated: false
      parameters:
        - name: flow
          in: body
          description: Each flow represents a subscriber flow into the VSG instance.
          schema:
            type: array
            items:
              description: Each flow represents a subscriber flow into the VSG instance.
              type: object
              properties:
                id:
                  type: string
                  format: 'yang:uuid'
          required: false
      responses:
        '200':
          description: Expected response for creating flow(s) in collection
          schema:
            type: array
            items:
              description: Each flow represents a subscriber flow into the VSG instance.
              type: object
              properties:
                id:
                  type: string
                  format: 'yang:uuid'
    get:
      summary: List all flows from gateway
      deprecated: false
      responses:
        '200':
          description: Expected response of flows
          schema:
            type: array
            items:
              description: Each flow represents a subscriber flow into the VSG instance.
              type: object
              properties:
                id:
                  type: string
                  format: 'yang:uuid'
    put:
      summary: Replace the entire flow collection
      deprecated: false
      parameters:
        - name: flow
          in: body
          description: Each flow represents a subscriber flow into the VSG instance.
          schema:
            type: array
            items:
              description: Each flow represents a subscriber flow into the VSG instance.
              type: object
              properties:
                id:
                  type: string
                  format: 'yang:uuid'
          required: false
      responses:
        '201':
          description: Expected response for replacing collection
    patch:
      summary: Merge items into the flow collection
      deprecated: false
      parameters:
        - name: flow
          in: body
          description: Each flow represents a subscriber flow into the VSG instance.
          schema:
            type: array
            items:
              description: Each flow represents a subscriber flow into the VSG instance.
              type: object
              properties:
                id:
                  type: string
                  format: 'yang:uuid'
          required: false
      responses:
        '201':
          description: Expected response for merging into collection
    parameters:
      - name: id
        in: path
        required: true
        description: A key uniquely identifying gateway item
        type: string
        format: 'xtype:unique-identifier'
  '/cord-vsg-service:controller/gateway/{id}/flow/{index}':
    get:
      description: Each flow represents a subscriber flow into the VSG instance.
      summary: View detail on flow
      deprecated: false
      responses:
        '200':
          description: Expected response of flow
          schema:
            description: Each flow represents a subscriber flow into the VSG instance.
            type: object
            properties:
              id:
                type: string
                format: 'yang:uuid'
    put:
      summary: Update details on flow
      deprecated: false
      parameters:
        - name: flow
          in: body
          description: Each flow represents a subscriber flow into the VSG instance.
          schema:
            description: Each flow represents a subscriber flow into the VSG instance.
            type: object
            properties:
              id:
                type: string
                format: 'yang:uuid'
          required: false
      responses:
        '200':
          description: Expected response of flow
          schema:
            description: Each flow represents a subscriber flow into the VSG instance.
            type: object
            properties:
              id:
                type: string
                format: 'yang:uuid'
    patch:
      summary: Merge details on flow
      deprecated: false
      parameters:
        - name: flow
          in: body
          description: Each flow represents a subscriber flow into the VSG instance.
          schema:
            description: Each flow represents a subscriber flow into the VSG instance.
            type: object
            properties:
              id:
                type: string
                format: 'yang:uuid'
          required: false
      responses:
        '200':
          description: Expected response of flow
          schema:
            description: Each flow represents a subscriber flow into the VSG instance.
            type: object
            properties:
              id:
                type: string
                format: 'yang:uuid'
    delete:
      summary: Delete flow from gateway
      deprecated: false
      responses:
        '204':
          description: Expected response for delete
    parameters:
      - name: index
        in: path
        required: true
        type: integer
        format: int64
        description: An index key identifying flow item (may change over time)
      - name: id
        in: path
        required: true
        description: A key uniquely identifying gateway item
        type: string
        format: 'xtype:unique-identifier'
definitions:
  'xslice:slice':
    description: >-
      A slice is a logically centralized container for network and compute
      resources
    allOf:
      - $ref: '#/definitions/xos:provider'
      - properties:
          slice-url:
            description: A URL describing the purpose of this slice
            type: string
            format: 'inet:uri'
          network:
            description: >-
              The network that the slice uses to connect to other slices and to
              the Internet.
            default: none
            type: string
            format: enumeration
            enum:
              - none
              - host
              - bridged
              - noauto
          creator:
            type: string
            format: leafref
          service:
            description: The service that runs in this slice
          instances:
            type: array
            items:
              type: string
              format: leafref
          networks:
            type: array
            items:
              type: string
              format: leafref
          ports:
            type: array
            items:
              description: The ports to be exposed for other slices to connect to
              type: integer
              format: uint16
          tags:
            type: array
            items:
              type: string
              format: string
          policy:
            type: object
            properties:
              max-instances:
                description: >-
                  The maximum number of VMs that this slice is allowed to
                  allocate
                type: integer
                format: uint16
              default-flavor:
                type: string
                format: leafref
              default-image:
                type: string
                format: leafref
              default-node:
                type: string
                format: leafref
              mount-data-sets:
                default: GenBank
                type: string
                format: string
              default-isolation:
                default: vm
                type: string
                format: 'xtype:isolation'
                enum:
                  - vm
                  - lxc
                  - hybrid
          scale:
            description: Adjust scale for slice(s)
            type: object
  'xos:provider':
    allOf:
      - $ref: '#/definitions/xos:tenant'
      - $ref: '#/definitions/xos:credentials-list'
  'xos:tenant':
    type: object
    required:
      - id
    properties:
      id:
        type: string
        format: 'xtype:unique-identifier'
      name:
        type: string
        format: string
      kind:
        type: string
        format: identityref
      description:
        type: string
        format: 'meta:description'
      enabled:
        default: 'true'
        type: boolean
        format: boolean
      record:
        $ref: '#/definitions/xos:record'
  'xos:record':
    description: Synchronizer-specific properties for model entities
    type: object
    properties:
      created:
        type: string
        format: 'yang:date-and-time'
      updated:
        type: string
        format: 'yang:date-and-time'
      enacted:
        type: string
        format: 'yang:date-and-time'
      policed:
        type: string
        format: 'yang:date-and-time'
      writable:
        default: 'true'
        type: boolean
        format: boolean
      locked:
        default: 'false'
        type: boolean
        format: boolean
      deleted:
        default: 'false'
        type: boolean
        format: boolean
      dirty:
        default: 'false'
        type: boolean
        format: boolean
      sync:
        type: object
        properties:
          progress:
            type: string
            format: enumeration
            enum:
              - provisioning
          disabled:
            default: 'false'
            type: boolean
            format: boolean
          enforced:
            default: 'true'
            type: boolean
            format: boolean
          policy:
            type: array
            items:
              type: object
      diff:
        description: retrieve diff of model state if dirty
        type: object
      save:
        description: trigger save into data store via synchronizer
        type: object
  'xos:credentials-list':
    type: object
    properties:
      credential:
        type: array
        items:
          type: object
          properties:
            token:
              type: string
              format: string
            password:
              type: string
              format: 'meta:password'
            user:
              type: string
              format: 'xtype:unique-identifier'
            role:
              default: user
              type: string
              format: 'xtype:access-role'
              enum:
                - admin
                - staff
                - user
  'xos:service':
    allOf:
      - $ref: '#/definitions/xos:provider'
      - properties:
          view-url:
            type: string
            format: 'inet:uri'
          icon-url:
            type: string
            format: 'inet:uri'
          published:
            default: 'true'
            type: boolean
            format: boolean
          service-specific-id:
            type: string
            format: 'xtype:unique-identifier'
          keypair:
            type: array
            items:
              description: collection of public/private key pair(s)
              type: object
              properties:
                public:
                  type: string
                  format: string
                private:
                  type: string
                  format: string
          attribute:
            type: array
            items:
              description: backwards-compatible attribute association
              type: object
              properties:
                name:
                  type: string
                  format: string
                value:
                  type: string
                  format: string
          subscriber:
            type: array
            items:
              description: >-
                Each entry represents a subscriber of the service. Each unique
                service

                provider should augment this block with service specific

                attributes.
              $ref: '#/definitions/xos:subscriber'
  'xos:subscriber':
    allOf:
      - $ref: '#/definitions/xos:tenant'
      - properties:
          connectivity:
            default: na
            type: string
            format: enumeration
            enum:
              - public
              - private
              - private-unidirectional
              - na
  gateways-list:
    type: object
    properties:
      gateway:
        type: array
        items:
          description: Each entry represents a VSG instance.
          $ref: '#/definitions/subscriber-gateway'
  subscriber-gateway:
    type: object
    properties:
      id:
        type: string
        format: 'xtype:unique-identifier'
      features:
        description: Contains various features provided by the gateway.
        type: object
        properties:
          firewall:
            type: object
            properties:
              enabled:
                default: 'false'
                type: boolean
                format: boolean
              rules:
                type: array
                items:
                  type: string
                  format: string
          url-filter:
            type: object
            properties:
              enabled:
                default: 'false'
                type: boolean
                format: boolean
              level:
                type: string
                format: enumeration
                enum:
                  - PG
              rules:
                type: array
                items:
                  type: string
                  format: string
          uverse:
            type: object
            properties:
              enabled:
                default: 'false'
                type: boolean
                format: boolean
      flow:
        type: array
        items:
          description: Each flow represents a subscriber flow into the VSG instance.
          type: object
          properties:
            id:
              type: string
              format: 'yang:uuid'
